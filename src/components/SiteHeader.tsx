import { NavBar } from "@/components/ui";

// Sprint 1.2 — Site header now wraps the design-system NavBar.
// Sprint 1.3 will wire real active-state detection from usePathname().
export function SiteHeader() {
  return <NavBar />;
}
